import 'dart:io';

Future<void> salvaHora() async {
  var arquivo = File('horas.txt');

  await arquivo.writeAsString(
    '${DateTime.now()}\n',
    mode: FileMode.append,
  );
}

void main() async {
  await salvaHora();
}