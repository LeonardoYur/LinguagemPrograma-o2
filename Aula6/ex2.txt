double divide(double x, double y) {
  if (y == 0) {
    throw Exception('Não é possível dividir por zero');
  }

  return x / y;
}

void main() {
  try {
    print(divide(2, 3));
    print(divide(2, 0));
  } catch (e) {
    print(e);
  }
}